{
  "agent": "critic",
  "description": "Tool allowlist for critic. Least privilege per \u00a734 - no generic shell, URL or secret access.",
  "allowed_tools": [
    "fetch_result",
    "request_validation_test",
    "fetch_dataset_coverage"
  ],
  "forbidden": [
    "shell",
    "http_request",
    "read_secret",
    "place_order",
    "write_dataset"
  ]
}
